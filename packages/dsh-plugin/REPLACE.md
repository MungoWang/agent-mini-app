插件 apply 时自动 listen `127.0.0.1:17880`（MONKEY_MINI_APP_HOST_PORT 可改）。
不必再跑 run-demo.sh。
日志：`[monkey-mini-app] apps host http://127.0.0.1:17880`
Client 列表走该 host `/api/apps`，app tab iframe `/app/<id>`。
