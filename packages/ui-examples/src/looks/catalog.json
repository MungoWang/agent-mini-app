{
  "looks": [
    {
      "id": "glass-island",
      "name": {
        "en": "Glass Island",
        "zh": "玻璃岛屿"
      },
      "when": "Personal home, lock-screen style one-shot. Type sits on the sky; two glass slabs dock at the bottom.",
      "whenNot": "Launchers, DataGrid, incident walls. Not a 2D mosaic.",
      "grammar": "poster-dock",
      "classes": {
        "root": "bg-background relative flex h-full min-h-0 flex-col gap-3 overflow-hidden p-5",
        "hero": "text-foreground",
        "slab": "rounded-3xl border",
        "wash": "pointer-events-none absolute inset-x-0 top-0 h-72 bg-[radial-gradient(620px_260px_at_16%_-6%,color-mix(in_oklch,var(--primary)_38%,transparent),transparent_68%)]",
        "content": "relative mx-auto flex h-full min-h-0 w-full max-w-5xl flex-col gap-3"
      },
      "theme": "themes/glass-island.css",
      "defaultFor": ["today"],
      "notes": "Liquid glass (SVG displacement + blur), not frost. `backdrop-filter: blur(2px) saturate(160%) url(#mma-liquid)` with feTurbulence→blur→displace (scale ~45, yChannel=B). Light ground is haze-grey + white + a whisper of blue with large floating orbs — not a saturated sky; dark is deep indigo. Sky needs grain + orb edges or the lens bends nothing. Only the island cluster is liquid; the working set is a soft card so the sky still frames the cluster (wall-to-wall glass is why the facade used to look nothing like the prototype). Radius is large (`--radius: 28px`) on the style object. Non-Chromium falls back to frost. Host bakes local theme.css vars into `/app/:id` first paint so the look is not near-white while mma-set-env is in flight.",
      "style": {
        "borderRadius": "var(--radius)",
        "backgroundColor": "color-mix(in oklch, var(--card) 14%, transparent)",
        "boxShadow": "inset 0 1px 0 card/60, inset 0 -1px 0 card/22, inset 0 0 24px card/14, 0 14px 36px -10px var(--shadow)",
        "border": "1px solid color-mix(in oklch, var(--card) 28%, transparent)",
        "backdropFilter": "blur(2px) saturate(160%) url(#mma-liquid)  /* Chromium; frost fallback otherwise */"
      }
    },
    {
      "id": "aurora-bento",
      "name": {
        "en": "Aurora Bento",
        "zh": "极光便当"
      },
      "when": "Launcher / overview. The page is a 6×3 tessellation, every cell a destination.",
      "whenNot": "A lock-screen home. No sky, no poster layer.",
      "grammar": "tessellation",
      "classes": {
        "root": "mx-auto grid w-full max-w-5xl grid-cols-6 gap-2.5 (spans written out, no sm:)",
        "tile": "bg-card relative rounded-2xl p-3 overflow-hidden + style={RAISED}",
        "hero": "bg-foreground text-background col-span-2 row-span-2 rounded-2xl p-4"
      },
      "defaultFor": ["board"],
      "theme": "themes/aurora-bento.css",
      "style": {
        "ground": "tight radial pools: --primary 92% (top-left), --primary mixed --foreground (top-right), --primary mixed --destructive (bottom) — token-derived, no literals",
        "RAISED": {
          "boxShadow": "0 1px 2px color-mix(in oklch, var(--foreground) 14%, transparent), 0 14px 30px -14px var(--shadow)"
        }
      },
      "notes": "A flat `bg-muted` ground turns this into an admin dashboard — the aurora IS the look. Three hue pools mixed out of the palette's own primary (toward --foreground for depth, toward --destructive for a warm drift) so no colour literal appears in ui.tsx, and they stay TIGHT (40-50% radii) so they read as light sources rather than a wallpaper wash. Cap the grid (`max-w-5xl mx-auto`) and gap it: an edge-to-edge tile wall hides the field, and the field is the look. Write spans out (`col-span-2`), never `sm:col-span-*` — the panel width is user-dragged. Tiles are OPAQUE and raised (contact + lift shadow): translucent tiles would collide with glass-island. The hero tile inverts to `bg-foreground text-background`. Every tile is a destination, never a stat readout. Light and dark may carry different hue families — this look's palette is indigo-violet by day, aurora green by night."
    },
    {
      "id": "desk-split",
      "name": {
        "en": "Desk Split",
        "zh": "精致分栏"
      },
      "when": "CRUD, tickets, contacts, ledgers. Two panes, hairline, dense toolbar.",
      "whenNot": "Marketing pages, lock-screen homes.",
      "grammar": "list-detail",
      "classes": {
        "root": "relative h-full min-h-0 bg-background (walnut wood, DESK wash overlay)",
        "pane": "cream paper: bg-card, hairline edge, contact + lift shadow, top highlight (PAPER)"
      },
      "defaultFor": ["sheets"],
      "style": {
        "backgroundImage": "repeating-linear-gradient(90deg, fg 5% 2px stripes / 96px bands), radial lamp pool from --primary at top-left, vignette at bottom",
        "paper": {
          "backgroundColor": "var(--card)",
          "boxShadow": "0 1px 2px fg/24, 0 18px 44px -18px var(--shadow), inset 0 1px 0 card/72",
          "borderColor": "fg/12"
        }
      },
      "notes": "The desk IS the palette: walnut `bg-background`, cream papers (`bg-card`) in BOTH modes — lamp-lit paper on a dark desk is the look; dark papers on wood read as mud. All ink (`foreground`) is dark in both modes and only ever lands on paper: chrome text (headers, captions) goes inside a paper strip, never directly on the wood. Wood grain = repeating stripe bands; one warm lamp pool from `--primary`; titles in ui-serif for the desk feel.",
      "theme": "themes/desk-split.css"
    },
    {
      "id": "editorial",
      "name": {
        "en": "Editorial",
        "zh": "编辑排版"
      },
      "when": "Weekly digest, insights, runbook, help. Type hierarchy, hairline rules, almost no cards.",
      "whenNot": "Monitor walls, dense tables.",
      "grammar": "prose-measure",
      "classes": {
        "root": "mx-auto max-w-2xl px-6 py-8",
        "kicker": "text-xs tracking-[0.2em] uppercase text-muted-foreground",
        "display": "font-serif text-3xl font-bold tracking-tight"
      },
      "defaultFor": ["radar"],
      "theme": "themes/editorial.css",
      "notes": "Paper and ink are the identity: under the default palette this look is just text on white and reads unfinished, so it ships a theme.css (newsprint + vermilion by day, cool night-reading stock + ultramarine by night). Keep the measure near 65ch, use hairlines (`border` / `Separator`) instead of cards, one accent only for links and emphasis, `font-serif` for display, and `first-letter:` for a drop cap. Radius stays small — this look is type and rules, not tiles."
    },
    {
      "id": "tape",
      "name": {
        "en": "Tape",
        "zh": "行情带"
      },
      "when": "Watch board, quotes, incident strip. Tabular numerals, vertical rules, colour = delta only.",
      "whenNot": "Homes, forms. No graph-paper grid, no card chrome.",
      "grammar": "numeral-strip",
      "classes": {
        "root": "h-full min-h-0 bg-background font-mono",
        "nums": "grid grid-cols-4 divide-x divide-border",
        "num": "px-4 py-5 text-3xl font-medium tracking-tight tabular-nums"
      },
      "defaultFor": ["watch"]
    },
    {
      "id": "void",
      "name": {
        "en": "Void",
        "zh": "黑空"
      },
      "when": "Named 'premium / sparse / AI product'. Huge type, one hairline, almost empty.",
      "whenNot": "Dense tables, watch boards. Opt-in only.",
      "grammar": "sparse-hero",
      "classes": {
        "root": "flex h-full min-h-0 flex-col justify-center px-8 py-10",
        "display": "max-w-[12ch] text-4xl font-medium tracking-tight",
        "hair": "mt-6 h-px w-28 bg-linear-to-r from-foreground to-transparent"
      },
      "defaultFor": []
    },
    {
      "id": "signage",
      "name": {
        "en": "Signage",
        "zh": "霓虹招牌"
      },
      "when": "Named cyberpunk / neon. Display type, chroma wash, caption columns — not glow cards.",
      "whenNot": "A dark dashboard with extra shadow. Opt-in only.",
      "grammar": "display-type",
      "classes": {
        "root": "relative h-full min-h-0 overflow-hidden bg-background p-6",
        "wash": "pointer-events-none absolute inset-0 bg-[radial-gradient(420px_180px_at_90%_10%,color-mix(in_oklch,var(--primary)_28%,transparent),transparent_62%)]",
        "display": "text-4xl font-bold tracking-tight leading-[0.95]"
      },
      "theme": "themes/signage.css",
      "defaultFor": []
    },
    {
      "id": "terminal",
      "name": {
        "en": "Terminal",
        "zh": "终端等宽"
      },
      "when": "Logs, agent progress, a command well. Mono rows, lamps, colour = state.",
      "whenNot": "Long-form reading.",
      "grammar": "mono-well",
      "classes": {
        "root": "flex h-full min-h-0 flex-col bg-background",
        "well": "min-h-0 flex-1 overflow-auto"
      },
      "theme": "themes/terminal.css",
      "defaultFor": ["runner", "chores"],
      "style": {
        "well": {
          "className": "rounded-lg border bg-card text-card-foreground",
          "textShadow": "0 0 6px color-mix(in oklch, var(--primary) 65%, transparent)",
          "overlay": "repeating-linear-gradient(0deg, foreground 7% hairlines every 3px) + radial corner vignette"
        }
      },
      "notes": "Phosphor-on-black CRT is the identity (green #3dff6e on #060904 in dark; a paper well with deep green ink in light). The Terminal block defaults to hard zinc-950 — override it with `bg-card text-card-foreground` (the app-side Tailwind theme has no `--color-surface`; use card tokens) or a host swap washes the well out (templates ship this look's theme.css for that reason). Scanlines + vignette sell the CRT; they live on an aria-hidden pointer-events-none overlay so they never eat clicks. Radius squared (8px): rounds read as a chat app, not a console. Colour = state, nothing else."
    }
  ]
}
