# @monkey-mini-app/sdk

Mini-app authoring SDK: `useApp` + the UI kit (re-exported from `@monkey-mini-app/ui`).

The host serves the iframe bundle as `/mma/sdk.js` (built by `scripts/build/sdk.mjs`).
